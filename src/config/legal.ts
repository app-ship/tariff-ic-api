/** Server-side terms version. The server always stamps its own version on the User
 *  record — it does not trust the client-supplied version value. */
export const CURRENT_TERMS_VERSION = '2026-06-30';
